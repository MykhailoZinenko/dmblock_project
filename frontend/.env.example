# Contract addresses: `sepolia` (default) = pinned Ethereum Sepolia deploy in src/deployments/sepolia.ts
# `anvil` = local addresses in src/deployments/anvil.ts (update there if your forge broadcast differs)
# VITE_CONTRACT_TARGET=anvil
