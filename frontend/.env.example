# Contract addresses: `sepolia` (default) = src/deployments/sepolia.ts
# Local: start Anvil, then from repo root: `bash scripts/redeploy-anvil.sh` (or `npm run redeploy:anvil` in frontend/)
# That writes anvil.ts + .env.local with VITE_CONTRACT_TARGET=anvil
# VITE_CONTRACT_TARGET=anvil
